

export const HOST = 'http://talos30364-prelb.o2athena.svc.ht1.n.jd.local'

// export const HOST = ''

const api = {
    task: `${HOST}/api/tasks`,
    diff: `${HOST}/api/v1/diff`,
    devices: `${HOST}/api/getDevices`,
    snapshot: `${HOST}/api/snapshot`,
}

export {
    api
}

